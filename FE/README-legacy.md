The frontend source has been relocated to `../frontend`.

This copy stays temporarily because an editor/dev server still holds the original `FE/` path. After you stop any running processes that use `FE/`, you can delete this folder and work exclusively inside `frontend/`.
